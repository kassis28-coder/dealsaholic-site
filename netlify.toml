[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Makes /.netlify/functions/get-deals also reachable at /api/deals
[[redirects]]
  from = "/api/deals"
  to = "/.netlify/functions/get-deals"
  status = 200
